# 로컬 개발 시에는 빈 상태로 두어도 됩니다.
# 프로덕션 배포 시 반드시 NODE_ENV=production 및 강력한 JWT_SECRET 를 설정하세요.

NODE_ENV=development
PORT=3000

# 32자 이상의 무작위 문자열 권장. 예: openssl rand -hex 32
JWT_SECRET=

# 영구 디스크 경로 (배포 플랫폼에서 마운트한 볼륨)
# DATA_DIR=/var/data
# UPLOADS_DIR=/var/data/uploads
